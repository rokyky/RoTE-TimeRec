## 新增需求

### 需求: 按用户分桶计算指标
系统必须支持按物品流行度、类目 entropy、序列长度等维度对用户分桶，分别计算 recall/ndcg。

#### 场景: 长尾分桶分析
- **当** 调用 evaluate_by_bucket() 并传入 popularity 分桶配置时
- **那么** 返回每个桶的指标，显示头部 vs 尾部性能差异

### 需求: 分桶阈值可配置
用户必须通过配置指定分桶列名和分位数阈值。

#### 场景: 自定义分桶阈值
- **当** 配置中提供分桶阈值时
- **那么** 用户按指定的分位数分组