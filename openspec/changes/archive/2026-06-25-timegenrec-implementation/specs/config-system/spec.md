## 新增需求

### 需求: 从 YAML 加载配置
系统必须读取 YAML 配置文件，解析模型超参、pipeline 阶段配置和数据路径。

#### 场景: 配置加载
- **当** 提供配置文件路径时
- **那么** 所有超参和阶段配置被解析为 dict

### 需求: 配置项有默认值
配置文件中缺失的字段必须使用合理默认值，而不是直接崩溃。

#### 场景: 部分配置使用默认值
- **当** 配置文件省略某些字段时
- **那么** 系统使用默认值并以 INFO 级别打印警告